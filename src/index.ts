import { yup } from "./translations/yup";
import { fields } from "./translations/fields";
import { misc } from "./translations/misc";
import { users } from "./translations/users";

export const polyglot = {
    yup,
    fields,
    misc,
    users
};